## Backend tests

This folder contains lightweight backend tests.

### Run (unittest)

From repo root:

```bash
source .venv/bin/activate
python -m unittest discover -s backend/tests -p "test_*.py"
```

